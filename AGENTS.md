<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# MCP server (AI ภายนอกอ่าน/แก้ diagram)

- MCP มี 2 transport แชร์ tools ชุดเดียวกัน (นิยามทั้งหมดใน `app/mcp/server.ts`): HTTP = `app/mcp/route.ts` (Streamable HTTP, stateless, `POST` เท่านั้น) AI เชื่อมที่ `http://localhost:3100/mcp` · stdio = `mcp-stdio.ts` ที่ root (`npm run mcp:stdio` — client spawn เอง ไม่ต้องรันเว็บ; script **chdir กลับ root ก่อน dynamic import** เสมอ เพราะ store หา `data/` จาก cwd ตอน import)
- **หลาย project**: source of truth = `data/projects.json` ผ่าน `app/store.ts` (atomic write + `rev` ต่อ project) — **ทุก MCP tool ต้องส่ง `project` (ชื่อ) เสมอ**; UI โหลด/เซฟผ่าน `app/api/projects/*`; Designer ส่ง storage signal หลัง save เพื่อ sync แท็บใน browser เดียวกันทันทีและ poll `rev` ทุก 1 วิเป็น fallback สำหรับคนละ browser/AI; `localStorage` เป็น offline cache และ sync signal เท่านั้น
- เพิ่ม tool ใหม่: `server.registerTool` ใน `createServer()` ของ `app/mcp/server.ts` — mutation ทุกตัวต้องจบด้วย `save(project, p)` เพื่อ auto save + เพิ่ม rev (ถ้าลืม UI จะไม่ refresh)
- **กฎคำอธิบายภาษาไทยเสมอ**: `add_collection`/`add_field` บังคับ `description` ไทย (zod required + `fieldsThaiError` เช็กอักขระไทย U+0E00–U+0E7F recursive ลง children); `update_collection`/`update_field` บังคับว่าหลังแก้ต้องเหลือคำอธิบายไทย; tool `check_descriptions` รายงานตัวที่ยังขาด; ฝั่ง UI บังคับใน `saveEditing` (`page.tsx`) ด้วย `isThaiText` จาก `schema.ts` + 💬 เหลืองเตือนจุดที่ขาด — ถ้าแก้ schema ของ field input อย่าลืมอัปเดต validation เส้นนี้ด้วย
- **error มี machine code** นำหน้าข้อความไทย เช่น `[PROJECT_NOT_FOUND]` `[DIAGRAM_NOT_FOUND]` `[COLLECTION_NOT_FOUND]` `[FIELD_NOT_FOUND]` `[DESCRIPTION_NOT_THAI]` `[DUPLICATE_LABEL]` `[RELATION_TARGET_GUIDFIXED]` — ใส่ code กับ error ใหม่ทุกจุดเสมอ · กฎห้ามอ้าง guidfixed ถูกบังคับจริงใน `add_relation`/`replace_diagram` (`guidfixedTargetError`) + กฎ lint `relation-to-guidfixed` (error) จับของเก่าที่หลุดมา · `[DESCRIPTION_NOT_THAI]` เติม hint อัตโนมัติเมื่อค่าที่รับเป็น `?` ล้วน (client ส่งไม่ใช่ UTF-8)
- **ห้าม recursive zod schema** ใน MCP tools (getter วนตัวเอง) — JSON Schema ที่ออกจะมี `$ref` ชี้ path ตรง ซึ่ง client บางตัวปฏิเสธทั้งชุด tools (Moonshot บังคับ `#/$defs/`, OpenAI strict ไม่รับ `$ref` เลย) → `fieldInputSchema` ใช้ children ลึกจำกัด **2 ชั้น**แบบ inline (ลดจาก 4 ชั้น: tools/list 68KB→50KB — ชั้นที่ 3 ตอบ `[FIELD_TOO_DEEP]` ไทยพร้อมทางหนี ให้ใช้ add_field + `parent` แบบ dotted path เติมทีละชั้น); ถ้าเพิ่ม tool ใหม่ที่มีโครงซ้อน ให้ทำแบบเดียวกัน · **เพดาน zod ทุกตัวต้องมีข้อความไทย + machine code** (`LIMIT_NAME`/`LIMIT_FIELDS`/`LIMIT_COLLECTIONS`/`LIMIT_RELATIONS` ใน server.ts) ไม่งั้น client เห็น zod error อังกฤษดิบ
- **เส้นข้าม tab**: เก็บใน diagram ต้นทางเสมอ (`move_collection` ย้าย node ฝั่ง source แล้วย้ายเส้นที่มันเป็นต้นทางตามไป diagram ปลายทางด้วย — รักษา invariant นี้); ปลายทางที่อยู่คนละ tab แสดงเป็น node เสมือน type `crossref` (การ์ดจางเส้นประ amber + ชื่อ tab + handle ครบทุก field ที่ถูกชี้, กดแล้วข้ามไป tab นั้น) — derive ตอน render จาก `allDiagrams` ไม่ persist, dedup ตาม target node; stub ครอบทั้งกรณี target และ source หลุดแท็บ (ข้อมูลเก่าที่เส้นค้างผิด diagram ยังเรนเดอร์ได้ ไม่หายเงียบ); ห้ามสร้าง stub ด้วย fields ว่างโดยไม่มี handle (เส้นจะไม่เรนเดอร์) · ลบ node/field แล้ว UI กวาดเส้นค้างในแท็บอื่น (`sweepCrossTabEdges` + toast 🧹) — **undo (Ctrl+Z) เป็น history ต่อ diagram ปัจจุบัน ไม่คืนเส้นที่ถูกกวาดในแท็บอื่น** (toast บอกผู้ใช้แล้ว; กู้ผ่าน MCP `list_revisions`/`restore_revision`)
- `add_collection` ปฏิเสธ label ซ้ำใน diagram เดียวกัน (เว้นแต่ `replace: true` = แทนที่ ลบเส้นเก่าด้วย); bulk import ทั้งผังใช้ `replace_diagram` (validate ก่อนทั้งหมดแล้วเขียน atomic rev +1 ครั้งเดียว) — ฝั่ง UI rename การ์ดก็บล็อกชื่อซ้ำ (revert + alert ใน onBlur ของ label input, `page.tsx`) และ label commit เข้า state ทุกตัวอักษร (onChange) ไม่รอ blur
- **UX ฝั่งเว็บ (set 2026-07-30)**: คลิกขวามีเมนูของแอปทั้ง การ์ด/เส้น/พื้นว่าง (`ctxMenu` ใน Designer — เมนูพื้นว่างถูกยิงตอน mouseup เพราะ `panOnDrag=[1,2]` แล้ว event contextmenu จริงตามมาโดน overlay: overlay ห้ามปิดเมนูใน onContextMenu ไม่งั้นเมนูหายก่อนเห็น); เมนูการ์ดมี "ย้ายไปแท็บ…" = `moveNodeToTab` ทำ semantics เดียวกับ MCP `move_collection` (เส้นที่ node เป็นต้นทางตามไป + **ต้องอัปเดต `diagramsMap.current[cur]` ก่อน `setAllDiagrams` ไม่งั้น crossref stub ไม่ขึ้นเพราะ node ค้างใน allDiagrams[cur]**); Ctrl+A เลือกทุก node, Ctrl+C/V คัดลอก/วาง, Ctrl+S ดักไว้ (toast บอก autosave), Ctrl+Z/Y ในช่องพิมพ์บนการ์ด = undo ของผัง (blur ก่อน) ส่วนช่องอื่นเป็น native undo; ปุ่ม ↶ enable ทันทีด้วย `dirty` (เทียบ snapshot กับ `present` ref); Ctrl+K ค้นเจอ collection แท็บอื่น (badge ชื่อแท็บ กดแล้วสลับ+ซูม); viewport จำต่อ diagram ใน localStorage (`mongomodel:vp:<id>` — openDiagram restore แทน fitView, ห้ามใส่ prop `fitView` กลับ ไม่งั้นทับ viewport ที่ restore); ปุ่ม Back เบราว์เซอร์ทำงานจริง (pushState + popstate ใน `App` — ข้าม mount แรกกัน push ทับ deep link); ลบ collection confirm เสมอ (onBeforeDelete); `GET/POST /api/revisions` = list/restore snapshot เดิมของ MCP เปิดให้ UI (ปุ่ม 🕘 ประวัติ — restore ย้อนทั้ง workspace); ปุ่ม 🩺 ตรวจต้องรวมทุก diagram และกดข้ามแท็บไปหา collection ได้ แต่คำเตือนหน้า codegen ใช้เฉพาะ diagram ปัจจุบัน; zoom ต่ำกว่า 0.25 ใช้ compact LOD โดยคง key/field ที่มีเส้นเชื่อมและเรียก `useUpdateNodeInternals` เมื่อสลับระดับ
- **ธง field**: `required` (*), `unique` (U), `key` (🔑 business key — field ที่ collection อื่นใช้อ้างอิง) — 🔑 แสดงข้างชื่อ field และเป็น marker เชิงออกแบบไม่สร้าง index; ทุกธง toggle ได้ในแถว field และส่งผ่าน MCP (`add_field`/`update_field`/`replace_diagram`) ได้; `_id` แสดง 🔑 อัตโนมัติ (PK ของ MongoDB); **กลุ่ม key (PK/🔑) อยู่บนสุดของ collection เสมอ** — toggle ติดหรือ add_field ด้วย key แล้ว field ถูก pin ขึ้นต่อท้ายกลุ่ม key อัตโนมัติ
- **แถบ key ด้านบนการ์ด** (KeyBar ใน `CollectionNodeView`): สรุป key ทั้งหมดของ collection (PK/🔑/⛓ key ผสม) ไว้ใต้ description เสมอ — **จุดเชื่อมเส้น (handle ทั้ง 4: `-s-l/-s-r/-t-l/-t-r`) ของ key field อยู่ที่แถบนี้ ไม่วาดซ้ำที่แถว field** (handle id เหมือนเดิม เส้นเก่าไม่พัง; `FieldRow` ข้าม handle เมื่อ `keyHandles`) เพื่อให้เห็นว่า collection มี key อะไรบ้างและลากเชื่อมจาก collection อื่นมาที่ key ได้ทันที; badge `← N` ท้ายแถว key = จำนวน collection ในแท็บปัจจุบันที่อ้างถึง key นั้น (derive จาก edges ตอน render ไม่ persist)
- **key ผสม (composite key, `field.keygroup`)**: fields ระดับบนที่มี `keygroup` (string id) เดียวกันรวมเป็น **key เดียว** — แถบ key แสดงเป็นกรอบกลุ่ม `⛓ key ผสม: a + b + c` (สมาชิกแต่ละตัวยังมี handle ของตัวเองและข้อมูล relation ยังเก็บ field→field เพื่อ codegen/lint; เมื่อมี mapping ≥2 คู่ระหว่าง keygroup คู่เดียวกันและมี kind/cardinality เดียวกัน Designer รวมภาพเป็น **เส้นเดียว** พร้อม label `a + b`; คลิก/คลิกขวาเปิด inspector ดู mapping ราย field; ลบ/เปลี่ยนชนิดเส้นสรุปต้องกระทบ mapping ทุกคู่; <2 สมาชิกแสดง ⚠) · ตั้ง/แก้กลุ่มด้วยปุ่ม ⛓ ในแถว field (popup เลือก/สร้างกลุ่ม ว่าง=ออกจากกลุ่ม) · เรียงลำดับสมาชิกด้วยปุ่ม ↑↓ ในกรอบ (ลำดับกลุ่มผูกกับลำดับ field จริง — compound index ออกตามนั้น) · เอาสมาชิกออกด้วย ✕ ท้ายแถว / ยกเลิกทั้งกลุ่มด้วย ✕ หัวกรอบ (fields ไม่ถูกลบ) · **โหมดกลุ่ม (`field.keygroupunique`, default true)**: ปุ่ม `ห้ามซ้ำ ⇄ ซ้ำได้` ที่หัวกรอบ — true = compound **unique** index, false = compound index ธรรมดาเพื่อค้นเร็ว (เก็บซ้ำทุกสมาชิก อ่านจากตัวแรก; codegen/markdown/wiki ออกตามโหมด) · สมาชิกกลุ่มห้ามซ้ำที่ไม่ required แสดง ⚠ (ค่าว่างชนกันเป็น duplicate null) · สมาชิก key ผสมถือเป็น key field (pin ขึ้นบน + แสดง ⛓ ในแถว field) · MCP รับ `keygroup`/`keygroupunique` ทุก tool · **ข้อควรรู้ MCP client**: client ที่ connect ค้างไว้จะมี tool schema เก่า (ไม่มี keygroup/keygroupunique) และ strip argument ทิ้งเงียบๆ — ต้อง reconnect/restart session ถึงจะใช้ผ่าน MCP ได้
- **index เพิ่มระดับ collection (`collection.data.indexes`)**: เก็บ `{id, fields:[{field:<field id>, direction:1|-1}], unique?, sparse?}`; ใช้ field id เพื่อให้ rename ไม่ทำ index หลุด ส่วน UI/MCP summary แสดง dotted path; ลบ field/แทน children/เปลี่ยนเป็นชนิดที่ไม่มี children ต้องลบทั้ง index ที่แตะสมาชิกนั้น ห้ามย่อ compound index เงียบ; codegen mongosh/Mongoose และ Markdown/Wiki ต้องออก index ชุดเดียวกัน พร้อม dedup กับ unique/keygroup/relation; compound ≤32 fields และ index รวม `_id`/unique/keygroup/relation/explicit ≤64; compound multikey ห้ามแตะ array คนละสาย; MCP รับ indexes ใน `add_collection`/`update_collection`/`replace_diagram` โดย input `field` รับ id หรือ dotted path และแปลงเป็น id ก่อน save
- **กฎ relation ห้ามอ้าง `guidfixed`** (set 2026-07-22 by Jead): เส้น relation ต้องออกจาก business key เท่านั้น (เช่น `code`/`holdingcode`/`businesscode`/`unitcode`) — `guidfixed` เป็น identity สำหรับยืนยัน/CRUD record ภายใน MongoDB เท่านั้น **ไม่พกพา** (export/import หรือย้ายเครื่องจะไม่ถูกย้ายไปด้วย) ถ้า relation อ้าง `guidfixed` ความสัมพันธ์จะพังหลังย้าย; master ที่ถูกอ้างต้องมี business key เสมอ (เช่น `holding` มี `holdingcode` ให้ `company.holdingcode` อ้าง); ทิศลูกศรวาดจากแม่→ลูก (arrowhead อยู่ปลายฝั่งลูก — markerStart ที่ field ต้นทาง + `orient: "auto-start-reverse"` ให้หัวลูกศรชี้เข้าหาลูก — default `auto` จะชี้ออกผิดทิศเป็นลูก→แม่; wiki graph `app/wiki/[project]/graph.tsx` ใช้ marker แบบเดียวกัน) — **dash animation ก็ต้องวิ่งแม่→ลูก**: path วาดจากลูก(source)→แม่(target) ทำให้ default animated วิ่งสวน ต้องใส่ `animationDirection: "reverse"` ใน style ของทุก edge — จุดบังคับเดียวคือ `displayEdges` (merge ทุกเส้นรวม edge เก่าที่ persist style **และ markerStart เก่าที่ไม่มี orient** ของตัวเองไว้ ทำให้ defaultEdgeOptions ไม่ถูกใช้ — displayEdges จึงสร้าง markerStart พร้อม orient ใหม่ให้ทุกเส้น) + style ใหม่ใน `defaultEdgeOptions`/`onEdgeDoubleClick` ต้องใส่ค่านี้เสมอ
- **กฎ relation เป็น field→field ระดับบนเสมอ** (set 2026-07-22 by Jead): เส้นต้องชี้ลงฟิลด์เป้าหมายระดับบนที่ถูกอ้างโดยตรง (`add_relation`/`replace_diagram` บังคับ `targetfield`) — field ซ้อนไม่มี handle และใช้ relation ไม่ได้; ข้อมูลเส้นเก็บ canonical `${fieldId}-s` → `${fieldId}-t` เสมอ ส่วนข้างของจุดเชื่อม (ซ้าย/ขวา) **เลือกอัตโนมัติตอน render** — ทุก field ระดับบนมี handle 2 ข้าง (`-s-l/-s-r`, `-t-l/-t-r`) และ `displayEdges` เลือกข้างที่หันเข้าหากันตามตำแหน่ง node (ลาก node แล้วเส้นสลับข้างเอง ไม่อ้อมหลังการ์ด); `onConnect` ต้อง normalize ตัด suffix `-l/-r` ก่อนเก็บ; จุดเชื่อมลอย**นอกการ์ด**แยกกัน (-s นอก 10px, -t amber นอก 24px) ไม่ทับไอคอนแถวและหัวลูกศร (markerStart ฝั่ง FK) เห็นชัด — ลากเริ่มได้ทั้ง 2 ฝั่ง React Flow จัดทิศตาม type เอง
- **Workflow ต้องแสดง Schema ที่ใช้** (set 2026-08-05 by Jead): แหล่งจริงคือ `step.dataAccess`; หน้า Workflow ต้องสรุปเฉพาะ Collection/Field/operation ที่ workflow ปัจจุบันอ้างถึงในแผง “Schema ที่เกี่ยวข้อง” และกดกลับไปยัง Schema ได้ ส่วน `get_workflow`/Markdown ต้องคืนสรุปชุดเดียวกัน ห้ามเดาความเกี่ยวข้องจากชื่อ workflow หรือแสดงทุก Collection เมื่อยังไม่ได้ผูก ให้แสดงสถานะว่างที่บอกวิธีเพิ่ม reference
- **Workflow auto-layout:** ปุ่ม `จัดผัง` ต้องใช้ ELK ที่มีอยู่ จัดทิศบน→ล่างโดยคำนึงถึงขนาด node, ใช้ handle เป้าหมายด้านบน/ต้นทางด้านล่าง, บันทึกตำแหน่งผ่าน history/autosave เดิม และ fit view หลังจัดเสร็จ ห้ามเพิ่ม layout dependency ซ้ำ
- **Auth schema ของ BC Ai Account**: `users` = บัญชีส่วนกลางและ password hash, `shopusers` = สมาชิก/บทบาท/ขอบเขตสิทธิ์รายโฮลดิ้ง, `shopuseraccesslogs` = audit การเข้าใช้; token/session จริงอยู่ Redis ไม่ใช่ MongoDB collection ส่วน `employee` เป็นข้อมูลบุคลากรและห้ามเก็บ password หรือใช้แทนบัญชี authentication


# Performance (cache/pool — ห้ามแก้โดยไม่เข้าใจ)

- `store.ts` cache workspace ตาม mtime+size (stat ทุกครั้ง) และ**คืน structuredClone เสมอ** — caller mutate ได้โดยไม่ทำ cache สกปรก; write แล้วอัปเดต cache ทันที; ไฟล์ไม่มี/migrate ต้อง bypass
- `wiki-data.ts` cache WikiData ตาม project.rev (key บน globalThis) — rev เปลี่ยนค่อยคำนวณใหม่
- REST ทั้ง 3 (`/api/projects`, `/api/projects/[name]`, `/api/wiki/[project]`) รองรับ `?rev=N` → 204 ว่างเมื่อ rev เดิม; UI poll ทุกจุดส่ง rev ด้วย
- `mcp/route.ts` ใช้ **server pool** แทนสร้าง McpServer ทุก request (วัดจริง createServer ~1.1ms/call) — connect ซ้ำหลัง close ได้เองเพราะ `transport.close()` เรียก onclose → `Protocol._onclose` ล้าง `_transport` ให้ (ตรวจกับ SDK 1.30.0 ใน node_modules แล้ว — `mcp.js`/`protocol.js` ไม่เปลี่ยนจาก 1.29.0; อัปเกรด SDK ให้ยิง POST /mcp ซ้ำหลายครั้งตรวจว่าไม่เจอ "Already connected to a transport"); concurrency ปลอดภัยเพราะ checkout เป็น sync pop · **Content-Type ตั้งแต่ SDK 1.30.0 ตรวจแบบ parsed media type** (ไม่ใช่ substring): client ต้องส่ง `application/json` (มี parameter เช่น `; charset=utf-8` ได้, ตัวพิมพ์ใหญ่เล็กไม่สน) ไม่งั้นได้ 415 — ห้ามไป normalize header ฝั่งเราให้หลวมเหมือน substring เดิม (จะเปิดช่อง CSRF ที่ SDK เพิ่งปิด)
- **trust boundary** (endpoint ไม่มี auth): `mcp/route.ts` เช็ก Origin ก่อนเข้า transport — มี Origin แต่ host ไม่ตรง → 403 `[FORBIDDEN_ORIGIN]` (กัน blind CSRF จากเว็บที่ผู้ใช้เปิด; MCP client ปกติไม่ส่ง Origin = ผ่าน ห้ามเข้มกว่านี้เดี๋ยว Claude Desktop/Cursor/Kimi พัง); `store.ts` อ่าน/เขียน `ws.projects` ผ่าน `readProject`/`writeProject` เท่านั้น (hasOwn + defineProperty) — ชื่อ project ที่ชน `Object.prototype` (`toString`/`__proto__`) ได้ error ไทยปกติ ไม่ TypeError/ไม่ pollute prototype; เพิ่มจุดอ่านเขียนใหม่ห้าม index ตรง
- codegen อยู่ใน `app/schema.ts` (pure) — มี `toWiki` ส่งออกชุดไฟล์ markdown โครงสร้าง wikillm (`Home.md` + `collections/` + `types/`) ทั้งใน UI (แท็บ Wiki) และ MCP (`generate_code` format `wiki`)
- หน้า wiki viewer แบบ Obsidian (เขียนเอง ไม่พึ่ง lib ภายนอก) อยู่ที่ `app/wiki/[project]/` — ใช้ 2 ทาง: แผงข้าง canvas ในหน้าเดียวกัน (ปุ่ม 📖 Wiki/🌐, โหลดข้อมูลจาก `GET /api/wiki/[project]`) และ route `/wiki/<ชื่อโปรเจกต์>` สำหรับลิงก์ตรง; parser อยู่ใน `note.tsx` (parse เฉพาะรูปแบบที่ `toWiki` ผลิต — ถ้าแก้ `toWiki` ต้องดู parser ด้วย); ข้อมูลเตรียมจาก `app/wiki-data.ts`
- ReactFlow ของ Designer และของ wiki graph ต้องอยู่คนละ `ReactFlowProvider` (ตอนนี้ provider ครอบ Designer ใน `App`) — รวมกันแล้ว store ชน เตือน node type not found

# Autonomous maintenance, verification, and deployment

- **ลุงจืดอนุญาตให้แก้ได้ทันที (set 2026-08-05):** เมื่อการใช้งานจาก BC Ai Account หรือ MCP พบ defect, semantic gap หรือสิ่งที่ควรปรับปรุงใน **MCP หรือส่วนใดก็ตามภายใน MongoModel** ให้แก้ root cause ใน repo นี้ได้ทันทีโดยไม่ต้องถามยืนยัน และห้ามหยุดแค่รายงานปัญหาหรือทำ workaround ฝั่ง caller ถ้าจุดที่ถูกต้องอยู่ที่นี่
- หลังแก้ **code หรือ behavior** ต้องรัน targeted regression (`npm test` เป็น baseline และเพิ่ม check เฉพาะจุดเมื่อจำเป็น) + `npm run build` จากนั้น rebuild และ deploy บน **Docker Desktop** ด้วย `npm run docker:up`; ต้องยืนยันว่า container `mongomodel` ทำงาน แล้วตรวจ `http://localhost:3100` และยิง MCP request จริงที่ `http://localhost:3100/mcp` ให้ผ่าน ไม่ใช่ตรวจแค่สถานะ container
- รักษา local changes ที่ไม่เกี่ยวข้องและใช้ Git ของ `D:\mongomodel` แยกจาก `D:\bccode`; สิทธิ์นี้ครอบคลุม edit/test/rebuild/deploy บน Docker Desktop แต่ไม่รวมเปิด port 3100 ออก LAN, production/R0, หรือ commit/push โดยอัตโนมัติ

# Docker

- รัน production ใน Docker Desktop: `npm run docker:up` (build + up -d), หยุด `npm run docker:down`, log `npm run docker:logs`
- `Dockerfile` เป็น multi-stage ตาม `output: "standalone"` ใน `next.config.ts` — รันด้วย `node server.js` ที่พอร์ต 3100 (ENV PORT/HOSTNAME)
- `docker-compose.yml` mount `./data:/app/data` — ข้อมูลโปรเจกต์อยู่บน host ลบ container ไม่หาย; ถ้าแก้โค้ดต้อง `docker:up` ใหม่ (build ใหม่ทุกครั้ง)
- port ผูก `127.0.0.1:3100:3100` เท่านั้น — /mcp กับ /api ไม่มี auth (ลบทั้ง project ได้) ห้ามเปิด bind 0.0.0.0 ให้ทั้ง LAN ยิง; ต้องการเปิดออก LAN จริงค่อยแก้ใน compose พร้อมยอมรับความเสี่ยง
- dev ปกติยังใช้ `npm run dev` ได้ (ห้ามรันชนกับ container — พอร์ตเดียวกัน)
